https://spring.io/guides/gs/spring-boot-docker

Gradle

# compilar
./gradlew build && java -jar build/libs/gs-spring-boot-docker-0.1.0.jar

# fer la image docker
docker build --build-arg JAR_FILE=build/libs/\*.jar -t angalemen/caballeroenapuros .

# docker login (Personal Acces Token)
docker push angalemen/caballeroenapuros



# datasourceulr ferlo en env
en el aplication.properties: 

spring.datasource.url=${DURL}

y en render environment ficarli ixa vairalbe $DURL
DURL = postgres://caballeroenapuros_user:zTfhO1koS7DUInRGlezONe1Y2g1Rpasv@dpg-crv7kdtds78s73aa17k0-a:5432/caballeroenapuros
  
  

